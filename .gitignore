# Environment variables
.env
.env.local
.env.* # Keep for flexibility in managing environment files

# Logs (Express apps often generate logs)
*.log
logs/

# Dependencies
node_modules/ # No changes, as it's already correct for Node.js projects

# OS files
.DS_Store # No changes, as it's relevant for macOS users
